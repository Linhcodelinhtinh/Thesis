name: training
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.2.0
    - torchvision>=0.17.0
    - transformers>=4.40.0
    - accelerate>=0.28.0
    - deepspeed>=0.14.0
    - peft>=0.10.0
    - wandb>=0.16.0
    - datasets>=2.18.0
    - tensorboard>=2.16.0
    - pyyaml>=6.0
    - pytest>=7.4.0
