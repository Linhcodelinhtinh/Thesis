Before modifying simulator configuration:

- inspect official config
- compare against pinned benchmark config
- identify whether change is:
  A. benchmark-preserving
  B. benchmark-derived
  C. custom experiment

Never mix A/B/C silently.